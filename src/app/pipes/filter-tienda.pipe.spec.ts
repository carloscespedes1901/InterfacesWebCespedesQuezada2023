import { FilterTiendaPipe } from './filter-tienda.pipe';

describe('FilterTiendaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTiendaPipe();
    expect(pipe).toBeTruthy();
  });
});
